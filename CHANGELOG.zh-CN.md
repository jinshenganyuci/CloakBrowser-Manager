# 更新记录

## 0.1.0-zh.6

- 将汉化范围收回到管理器的操作和创建 / 编辑界面；新配置语言、时区恢复原版的留空默认值，避免干扰 GeoIP。
- 撤掉 Docker 全局中文语言设置、KeePassXC 强制中文和额外 Qt 翻译打包；远程浏览器默认书签恢复上游文本。
- 保留另行要求的中文输入、双向剪贴板、地区下拉菜单和明确选择语言后的运行时支持；已有配置及用户数据保留。
- 更新界面说明和回归检查，区分界面文字、配置默认值和远程程序行为。

## 0.1.0-zh.5

- 语言切换同时更新 Chromium 首选语言列表，清除旧配置残留的语言选择；保留其他浏览器偏好设置。
- 真实浏览器检查增加首选语言列表验证。

## 0.1.0-zh.4

- 修复选择外语后远程 Chromium 菜单、设置页仍是中文的问题：Linux 浏览器界面语言按每个配置单独设置，不再被 Docker 全局中文环境覆盖。
- 保留 UTF-8 和中文输入支持；管理界面及 KeePassXC 仍为中文。
- 增加真实浏览器回归检查，覆盖中/日/德/英界面、网页语言、Accept-Language、时区及夏令时，并复用已有数据目录验证切换。

## 0.1.0-zh.3

- 时区和浏览器语言改为明确的下拉选择，增加美国、日本、德国等常用地区；美国提供四个常用时区。
- 保留自定义输入和留空选项，兼容已有配置；手机上改为上下排列。

## 0.1.0-zh.2

- 点击远程画面后直接使用本机中文输入法，自动提交已确认的文字，支持地址栏。
- 支持普通 HTTP 下 Ctrl/Cmd+C、X、V 双向文字复制、剪切和粘贴。
- 同步远程右键/网页复制按钮产生的文字；焦点移出后停止后台同步，忽略本机输入的回传。
- 使用真实 X11 剪贴板，移除可能读取旧标签页内容的 JavaScript 缓存。
- 新增剪贴板就绪检查，键盘和粘贴快捷键统一走 RFB，按顺序处理中文与后续按键。
- 增加输入法合成、剪贴板、修饰键顺序、断线清理与 HTTP 实际浏览器回归检查。

## 0.1.0-zh.1

- 基于 CwithW `2d6ab81` 创建中文分支，保留 KeePassXC、自动解锁、指纹和浏览器生命周期功能。
- 完整汉化管理界面、操作状态、工具提示、表单和错误消息，补齐中文无障碍标签。
- 新配置默认 `zh-CN` / `Asia/Shanghai`；提供简繁中文语言和常见时区选项。
- 增加支持本机输入法的远程文本输入面板，支持简繁中文、Emoji 和多行粘贴。
- 修复输入法确认候选词时提前添加标签和启动参数的问题。
- 增加 CJK / Emoji 字体、中文 UTF-8 运行环境、中文默认书签和明确的 UTF-8 剪贴板传输。
- 补齐 KeePassXC 缺少的 Qt 简体中文语言包，并单独设置其界面语言与 UTF-8 进程环境。
- 补充中文配置读写、输入法和错误处理回归测试及 GitHub Actions；更新兼容范围内的前端依赖锁定版本。
- 中文 README 作为默认文档，保留英文上游说明和原始许可文件。
